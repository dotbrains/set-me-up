{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "set-me-up release readiness",
  "type": "object",
  "required": ["mode", "stage", "dry_run", "candidate", "release", "publish_plan", "validated", "pushed", "tagged", "failed", "provenance", "repositories"],
  "properties": {
    "mode": { "enum": ["check", "push", "publish-plan", "candidate-check", "self-test"] },
    "stage": { "type": "string" },
    "dry_run": { "type": "boolean" },
    "candidate": {
      "type": "object",
      "required": ["ref", "head", "target", "fresh", "age_days", "max_age_days", "age_ok"],
      "properties": {
        "ref": { "type": "string" },
        "head": { "type": "string" },
        "target": { "type": "string" },
        "fresh": { "type": "boolean" },
        "age_days": { "type": "integer" },
        "max_age_days": { "type": "integer" },
        "age_ok": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "release": {
      "type": "object",
      "required": ["tag", "signed", "github_release", "notes_file"],
      "properties": {
        "tag": { "type": "string" },
        "signed": { "type": "boolean" },
        "github_release": { "type": "boolean" },
        "notes_file": { "type": "string" }
      },
      "additionalProperties": false
    },
    "publish_plan": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["action", "target", "detail"],
        "properties": {
          "action": { "type": "string" },
          "target": { "type": "string" },
          "detail": { "type": "string" }
        },
        "additionalProperties": false
      }
    },
    "validated": { "type": "boolean" },
    "pushed": { "type": "boolean" },
    "tagged": { "type": "boolean" },
    "failed": { "type": "integer" },
    "provenance": {
      "type": "object",
      "required": ["timestamp", "workflow_run", "installer", "candidate", "tag", "blueprint", "tests"],
      "properties": {
        "timestamp": { "type": "string" },
        "workflow_run": { "type": "string" },
        "installer": { "type": "string" },
        "candidate": { "type": "string" },
        "tag": { "type": "string" },
        "blueprint": { "type": "string" },
        "tests": { "type": "string" }
      },
      "additionalProperties": false
    },
    "repositories": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["path", "branch", "head", "clean", "sync"],
        "properties": {
          "path": { "enum": ["installer", "blueprint", "tests"] },
          "branch": { "type": "string" },
          "head": { "type": "string" },
          "clean": { "type": "boolean" },
          "sync": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
